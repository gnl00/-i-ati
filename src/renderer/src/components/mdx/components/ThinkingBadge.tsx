import { Badge } from "@renderer/components/ui/badge"

function ThinkingBadge() {
  return <Badge variant={'secondary'} className="select-none bg-gray-100 text-gray-500">Thinking</Badge>
}

export default ThinkingBadge