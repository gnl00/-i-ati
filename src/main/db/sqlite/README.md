# sqlite

`sqlite/` 放 SQLite 专用适配与底层集成辅助。

适合放在这里的内容：
- SQLite 扩展加载
- SQLite 方言或能力探测
- 与具体驱动、原生扩展绑定的初始化辅助

不应该放在这里的内容：
- 领域对象读写
- 通用业务逻辑
- 与某张业务表强绑定的查询封装

判断标准：
- 如果代码离开 SQLite 就失去意义，它适合放在 `sqlite/`
- 如果代码在讨论 chat、message、plugin、task 这类领域概念，它通常不该在这里
