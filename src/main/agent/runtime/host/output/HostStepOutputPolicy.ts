/**
 * HostStepOutputPolicy
 *
 * 放置内容：
 * - step -> visible / hidden / tool-activity-only 的输出规则
 *
 * 业务逻辑边界：
 * - 这是 host-facing output policy，不是 core runtime policy
 * - 它可以决定某个 step 不产生任何 output
 */
export {}
